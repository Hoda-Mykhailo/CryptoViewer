<Window x:Class="CryptoViewer.View.DetailsView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Crypto Details"
        Height="450"
        Width="600">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibility"/>
    </Window.Resources>

    <Grid Margin="20">
        <StackPanel>
            <TextBlock Text="{Binding Currency.Name}"
                       FontSize="26"
                       FontWeight="Bold"/>

            <TextBlock Text="{Binding Currency.Symbol}"
                       FontSize="16"
                       Opacity="0.7"/>

            <TextBlock Text="{Binding Currency.Price, StringFormat=Price: {0:C}}"
                       FontSize="20"
                       Margin="0,10,0,0"/>

            <TextBlock Text="{Binding Currency.Change24h, StringFormat=24h: {0}%}"
                       Foreground="Green"/>

            <TextBlock Text="Loading chart..."
                   Margin="0,20,0,0"
                   Visibility="{Binding IsLoading,
                   Converter={StaticResource BoolToVisibility}}"/>
        </StackPanel>
    </Grid>
</Window>
